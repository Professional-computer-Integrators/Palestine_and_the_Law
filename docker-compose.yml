services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
    volumes:
      - app_data:/app/data
    restart: unless-stopped

volumes:
  app_data:
